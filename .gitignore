# Logs
/logs
*.log
# …but example .log fixtures are part of the repo (demos + test fixtures).
!examples/*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Tauri updater signing key. The public key is embedded in tauri.conf.json;
# this private key must only live locally and in GitHub Actions secrets.
.tauri/*.key
.tauri/*.key.pub

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
codebase-review.md
